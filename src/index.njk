{# Hero Section #}
<section id="home" class="hero">
    <div class="container hero-content">
        <img src="{{ pageData.headshot }}" alt="{{ pageData.footer_section.name }} Headshot" class="headshot">
        <div>
            <h1 data-lang-key="heroTitle">{{ pageData.hero_section.title_sv }}</h1>
            <p class="subtitle" data-lang-key="heroSubtitle">{{ pageData.hero_section.subtitle_sv }}</p>
            <div class="hero-buttons">
                 <a href="#contact" class="button" data-lang-key="heroContactBtn">{{ pageData.hero_section.contact_btn_sv }}</a>
                 <a href="{{ pageData.cv_sv }}" target="_blank" rel="noopener noreferrer" class="button secondary" data-lang-key="heroCvBtn" id="cv-download-link">{{ pageData.hero_section.cv_btn_sv }}</a>
            </div>
        </div>
    </div>
</section>

{# About Section #}
<section id="about" class="about section-padding">
    <div class="container">
        <h2 data-lang-key="aboutTitle">{{ pageData.about_section.title_sv }}</h2>
        <p data-lang-key="aboutText1">{{ pageData.about_section.text1_sv }}</p>
        <p data-lang-key="aboutText2">{{ pageData.about_section.text2_sv }}</p>
        <h3 data-lang-key="skillsTitle">{{ pageData.about_section.skills_title_sv }}</h3>
        <ul class="skills">
            {% for item in pageData.about_section.skills %}
            <li data-lang-key="skill_{{ loop.index }}">{{ item.skill_sv }}</li>
            {% endfor %}
        </ul>
    </div>
</section>

{# Experience Section #}
<section id="experience" class="experience section-padding alt-bg">
    <div class="container">
        <h2 data-lang-key="experienceTitle">{{ pageData.experience_section.title_sv }}</h2>
        <div class="timeline">
            {% for job in pageData.experience_section.jobs %}
            <div class="timeline-item card">
                <h3 data-lang-key="exp{{ loop.index }}Title">{{ job.title_sv }}</h3>
                <p class="company">{{ job.company }}</p>
                <p class="dates" data-lang-key="exp{{ loop.index }}Dates">{{ job.dates_sv }}</p>
                <ul data-lang-key="exp{{ loop.index }}Desc">
                    {% for desc_item in job.desc_sv %}
                    <li>{{ desc_item }}</li>
                    {% endfor %}
                </ul>
            </div>
            {% endfor %}
        </div>
    </div>
</section>

{# Education Section #}
<section id="education" class="education section-padding">
    <div class="container">
        <h2 data-lang-key="educationTitle">{{ pageData.education_section.title_sv }}</h2>
        <div class="timeline">
            {% for edu in pageData.education_section.educations %}
             <div class="timeline-item card">
                <h3 data-lang-key="edu{{ loop.index }}Title">{{ edu.title_sv }}</h3>
                <p class="institution">{{ edu.institution }}</p>
                <p class="dates" data-lang-key="edu{{ loop.index }}Dates">{{ edu.dates_sv }}</p>
                {% if edu.details_sv %}
                <p class="details" data-lang-key="edu{{ loop.index }}Details">{{ edu.details_sv }}</p>
                {% endif %}
            </div>
            {% endfor %}
        </div>
         <h3 data-lang-key="languagesTitle">{{ pageData.education_section.lang_title_sv }}</h3>
         <div class="language-info">
            {% for lang in pageData.education_section.languages %}
             <p><strong data-lang-key="lang{{ loop.index }}Name">{{ lang.name_sv }}:</strong> <span data-lang-key="lang{{ loop.index }}Level">{{ lang.level_sv }}</span></p>
            {% endfor %}
         </div>
    </div>
</section>

{# Contact Section #}
<section id="contact" class="contact section-padding alt-bg">
    <div class="container">
        <h2 data-lang-key="contactTitle">{{ pageData.contact_section.title_sv }}</h2>
        <p class="contact-intro" data-lang-key="contactIntro">{{ pageData.contact_section.intro_sv }}</p>
        <div class="contact-info">
            <p><strong data-lang-key="contactEmailLabel">{{ pageData.contact_section.email_label_sv }}</strong> <a href="mailto:{{ pageData.contact_section.email_address }}">{{ pageData.contact_section.email_address }}</a></p>
            <p><strong data-lang-key="contactPhoneLabel">{{ pageData.contact_section.phone_label_sv }}</strong> <a href="tel:{{ pageData.contact_section.phone_number | replace(' ', '') }}">{{ pageData.contact_section.phone_number }}</a></p>
            <p><strong data-lang-key="contactLinkedInLabel">{{ pageData.contact_section.linkedin_label_sv }}</strong> <a href="{{ pageData.contact_section.linkedin_url }}" target="_blank" rel="noopener noreferrer" data-lang-key="contactLinkedInLinkText">{{ pageData.contact_section.linkedin_link_text_sv }}</a></p>
            <p><strong data-lang-key="contactLocationLabel">{{ pageData.contact_section.location_label_sv }}</strong> <span data-lang-key="contactLocationText">{{ pageData.contact_section.location_text_sv }}</span></p>
        </div>
    </div>
</section>